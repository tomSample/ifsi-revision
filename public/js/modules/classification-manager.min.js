class ClassificationManager{constructor(auth,db){this.auth = auth;this.db = db;this.cache = new Map(); / / Cache local pour les classifications this.cacheTimeout = 3600000; / / 1 heure}async hasUserVoted(termId){const user = this.auth.currentUser;if ( ! user) return false;try{const{getDoc,doc} = await import('https: / / www.gstatic.com / firebasejs / 10.7.0 / firebase - firestore.js');const progressRef = doc(this.db,'userProgress',`${user.uid}_${termId}`);const progressDoc = await getDoc(progressRef);return progressDoc.exists() & & progressDoc.data().hasVoted = = = true;}catch (error){console.error('Erreur vérification vote:',error);return false;}}async voteForTerm(termId,importance){const user = this.auth.currentUser;if ( ! user){throw new Error('Utilisateur non connecté');}const hasVoted = await this.hasUserVoted(termId);if (hasVoted){console.warn('L\'utilisateur a déjà voté pour ce terme');return;}try{const{runTransaction,doc,getDoc,setDoc,updateDoc} = await import('https: / / www.gstatic.com / firebasejs / 10.7.0 / firebase - firestore.js');await runTransaction(this.db,async (transaction) = > {const classificationRef = doc(this.db,'termClassifications',termId);const classificationDoc = await transaction.get(classificationRef);if ( ! classificationDoc.exists()){transaction.set(classificationRef,{termId,votes:{essential:importance = = = 'essential' ? 1:0,important:importance = = = 'important' ? 1:0,supplementary:importance = = = 'supplementary' ? 1:0,ignored:importance = = = 'ignored' ? 1:0},totalVotes:1,majorityChoice:importance,majorityPercentage:100,lastUpdated:new Date().toISOString(),createdAt:new Date().toISOString()});}else{const data = classificationDoc.data();data.votes[importance] + + ;data.totalVotes + + ;const maxVotes = Math.max(...Object.values(data.votes));const majorityChoice = Object.keys(data.votes).find( key = > data.votes[key] = = = maxVotes );const majorityPercentage = Math.round((maxVotes / data.totalVotes) * 100);transaction.update(classificationRef,{votes:data.votes,totalVotes:data.totalVotes,majorityChoice,majorityPercentage,lastUpdated:new Date().toISOString()});}const progressRef = doc(this.db,'userProgress',`${user.uid}_${termId}`);transaction.set(progressRef,{termId,userId:user.uid,personalImportance:importance,hasVoted:true,votedAt:new Date().toISOString()},{merge:true});});this.cache.delete(termId);console.log(`✅ Vote enregistré:${importance}pour ${termId}`);}catch (error){console.error('Erreur lors du vote:',error);throw error;}}async modifyVote(termId,newImportance){const user = this.auth.currentUser;if ( ! user){throw new Error('Utilisateur non connecté');}try{const{runTransaction,doc} = await import('https: / / www.gstatic.com / firebasejs / 10.7.0 / firebase - firestore.js');await runTransaction(this.db,async (transaction) = > {const progressRef = doc(this.db,'userProgress',`${user.uid}_${termId}`);const progressDoc = await transaction.get(progressRef);if ( ! progressDoc.exists() | | ! progressDoc.data().hasVoted){throw new Error('L\'utilisateur n\'a pas encore voté pour ce terme');}const oldImportance = progressDoc.data().personalImportance;if (oldImportance = = = newImportance){return;}const classificationRef = doc(this.db,'termClassifications',termId);const classificationDoc = await transaction.get(classificationRef);if (classificationDoc.exists()){const data = classificationDoc.data();data.votes[oldImportance] - - ;data.votes[newImportance] + + ;const maxVotes = Math.max(...Object.values(data.votes));const majorityChoice = Object.keys(data.votes).find( key = > data.votes[key] = = = maxVotes );const majorityPercentage = Math.round((maxVotes / data.totalVotes) * 100);transaction.update(classificationRef,{votes:data.votes,majorityChoice,majorityPercentage,lastUpdated:new Date().toISOString()});}transaction.update(progressRef,{personalImportance:newImportance,lastModifiedAt:new Date().toISOString()});});this.cache.delete(termId);console.log(`✅ Vote modifié:${newImportance}pour ${termId}`);}catch (error){console.error('Erreur lors de la modification du vote:',error);throw error;}}async getClassificationInfo(termId){const cached = this.cache.get(termId);if (cached & & Date.now() - cached.timestamp < this.cacheTimeout){return cached.data;}const user = this.auth.currentUser;try{const{getDoc,doc} = await import('https: / / www.gstatic.com / firebasejs / 10.7.0 / firebase - firestore.js');const classificationRef = doc(this.db,'termClassifications',termId);const classificationDoc = await getDoc(classificationRef);let globalInfo = null;if (classificationDoc.exists()){const data = classificationDoc.data();globalInfo = {majorityChoice:data.majorityChoice,majorityPercentage:data.majorityPercentage,totalVotes:data.totalVotes,votes:data.votes};}let personalInfo = null;if (user){const progressRef = doc(this.db,'userProgress',`${user.uid}_${termId}`);const progressDoc = await getDoc(progressRef);if (progressDoc.exists()){personalInfo = {importance:progressDoc.data().personalImportance,hasVoted:progressDoc.data().hasVoted | | false,votedAt:progressDoc.data().votedAt};}}const result = {global:globalInfo,personal:personalInfo};this.cache.set(termId,{data:result,timestamp:Date.now()});return result;}catch (error){console.error('Erreur récupération classification:',error);return{global:null,personal:null};}}async getUserClassificationStats(){const user = this.auth.currentUser;if ( ! user) return null;try{const{collection,query,where,getDocs} = await import('https: / / www.gstatic.com / firebasejs / 10.7.0 / firebase - firestore.js');const progressRef = collection(this.db,'userProgress');const q = query(progressRef,where('userId',' = = ',user.uid),where('hasVoted',' = = ',true));const snapshot = await getDocs(q);const stats = {totalVoted:0,essential:0,important:0,supplementary:0,ignored:0};snapshot.forEach((doc) = > {const data = doc.data();if (data.personalImportance){stats.totalVoted + + ;stats[data.personalImportance] + + ;}});return stats;}catch (error){console.error('Erreur récupération stats:',error);return null;}}static getImportanceBadge(importance){const badges = {essential:'🔴',important:'🟡',supplementary:'🟢',ignored:'⚫'};return badges[importance] | | '';}static getImportanceLabel(importance){const labels = {essential:'Essentiel',important:'Important',supplementary:'Complémentaire',ignored:'Ignoré'};return labels[importance] | | '';}static getImportanceDescription(importance){const descriptions = {essential:'Incontournable pour l\'examen',important:'Utile à connaître',supplementary:'Culture générale',ignored:'Ne plus voir ce terme'};return descriptions[importance] | | '';}}if (typeof module ! = = 'undefined' & & module.exports){module.exports = ClassificationManager;}
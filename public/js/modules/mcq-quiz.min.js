class McqQuiz{constructor(){this.quizData = [];this.currentQuiz = [];this.currentQuestionIndex = 0;this.score = 0;this.quizActive = false;this.questionsPerQuiz = 20;this.userAnswers = [];}async loadQuizData(filePath){try{const paths = [ '.. / .. / data / questionnaire / UE_4.4.S2_antibiotiques.json',' / src / data / questionnaire / UE_4.4.S2_antibiotiques.json',(window.resolvePath ? window.resolvePath(' / src / data / questionnaire / UE_4.4.S2_antibiotiques.json'):null) ].filter(p = > p ! = = null);let jsonData = null;let loadedFrom = null;for (const path of paths){try{console.log(`[McqQuiz] Essai de charger:${path}`);const response = await fetch(path);if (response.ok){jsonData = await response.json();loadedFrom = path;console.log(`✓ Chargé depuis:${path}`);break;}}catch (err){console.log(` ✗ Échec:${err.message}`);continue;}}if ( ! jsonData){throw new Error('Impossible de charger le fichier JSON');}this.quizData = jsonData;console.log(`✓ Quiz chargé:${this.quizData.length}questions`);return true;}catch (error){console.error('Erreur chargement quiz:',error);return false;}}generateQuiz(count,filters = {}){let questions = [...this.quizData];if (filters.type & & filters.type.length > 0){questions = questions.filter(q = > filters.type.includes(q.type));}if (filters.theme & & filters.theme.length > 0){questions = questions.filter(q = > filters.theme.includes(q.theme));}questions = this.shuffleArray(questions).slice(0,Math.min(count,questions.length));this.currentQuiz = questions.map(q = > {return{...q,variante:q.variantes[Math.floor(Math.random() * q.variantes.length)],reponsesMelangees:this.shuffleArray([...q.reponses])};});this.currentQuestionIndex = 0;this.score = 0;this.userAnswers = new Array(this.currentQuiz.length).fill(null);this.quizActive = true;console.log(`▶ Quiz généré:${this.currentQuiz.length}questions`);return this.currentQuiz;}getCurrentQuestion(){if (this.currentQuestionIndex > = this.currentQuiz.length){return null;}return this.currentQuiz[this.currentQuestionIndex];}setAnswer(answer){if (this.currentQuestionIndex < this.userAnswers.length){this.userAnswers[this.currentQuestionIndex] = answer;}}nextQuestion(){if (this.currentQuestionIndex < this.currentQuiz.length - 1){this.currentQuestionIndex + + ;return true;}return false;}previousQuestion(){if (this.currentQuestionIndex > 0){this.currentQuestionIndex - - ;return true;}return false;}calculateScore(){this.score = 0;for (let i = 0;i < this.currentQuiz.length;i + + ){if (this.userAnswers[i] = = = this.currentQuiz[i].correcte){this.score + + ;}}this.quizActive = false;return this.score;}getResults(){const results = [];for (let i = 0;i < this.currentQuiz.length;i + + ){results.push({question:this.currentQuiz[i].variante,userAnswer:this.userAnswers[i],correctAnswer:this.currentQuiz[i].correcte,isCorrect:this.userAnswers[i] = = = this.currentQuiz[i].correcte,theme:this.currentQuiz[i].theme,type:this.currentQuiz[i].type});}return results;}getThemes(){const themes = [...new Set(this.quizData.map(q = > q.theme))];return themes.sort();}getTypes(){const types = [...new Set(this.quizData.map(q = > q.type))];return types.sort();}shuffleArray(array){const shuffled = [...array];for (let i = shuffled.length - 1;i > 0;i - - ){const j = Math.floor(Math.random() * (i + 1));[shuffled[i],shuffled[j]] = [shuffled[j],shuffled[i]];}return shuffled;}reset(){this.currentQuiz = [];this.currentQuestionIndex = 0;this.score = 0;this.quizActive = false;this.userAnswers = [];}}
let currentQuizType = null; / / 'pharma' ou 'mcq' let quizInstance = null; / / Instance du quiz (PharmaQuiz ou McqQuiz) let currentAnswerSelected = null;document.addEventListener('DOMContentLoaded',async () = > {console.log('📚 Initialisation de l\'application quiz...');showScreen('quizSelectionScreen');});window.selectQuiz = async function(quizType){console.log(`📖 Sélection du quiz:${quizType}`);currentQuizType = quizType;showScreen('loadingScreen');try{if (quizType = = = 'UE_4.4.S2_antibiotiques'){quizInstance = new McqQuiz();const filePath = ' / src / data / questionnaire / UE_4.4.S2_antibiotiques.json';const loaded = await quizInstance.loadQuizData(filePath);if (loaded & & quizInstance.quizData.length > 0){console.log('✓ Quiz Antibiotiques chargé');setupMcqUI();populateMcqFilters();showScreen('startScreen');}else{throw new Error('Questions antibiotiques non chargées');}}else{throw new Error('Quiz non trouvé');}}catch (error){console.error('Erreur:',error);showError(`❌ Erreur:${error.message}`);}};function setupPharmaCourseUI(){const startScreen = document.getElementById('startScreen');startScreen.innerHTML = ` < div class = "start - screen" > < div class = "start - icon" > 💊 < / div > < h2 > Configurez votre quiz de pharmacologie < / h2 > < p > Prêt à réviser? < / p > < div class = "quiz - options" > < div class = "option - group" > < label > < input type = "number" id = "questionsCount" value = "10" min = "5" max = "50" style = "width:60px;padding:0.5rem;" > < strong > Nombre de questions < / strong > < / label > < / div > < / div > < div class = "filters - section" > < div class = "filters - title" > 🎯 Filtrer par familles et domaines (tous sélectionnés par défaut) < / div > < div class = "filters - grid" > < div class = "filter - group" > < h3 > Familles de médicaments < / h3 > < div class = "filter - options" id = "familiesContainer" > < / div > < / div > < div class = "filter - group" > < h3 > Domaines d'étude < / h3 > < div class = "filter - options" id = "domainsContainer" > < / div > < / div > < / div > < / div > < div class = "quiz - actions" > < button class = "btn btn - secondary" onclick = "window.backToSelection()" > ← Changer de quiz < / button > < button class = "btn btn - primary" onclick = "window.startPharmaCourseQuiz()" > Démarrer le Quiz < / button > < / div > < / div > `;}function setupMcqUI(){const startScreen = document.getElementById('startScreen');startScreen.innerHTML = ` < div class = "start - screen" > < div class = "start - icon" > 🔬 < / div > < h2 > Configurez votre quiz d'antibiotiques < / h2 > < p > Testez vos connaissances en mémorisation et sécurité < / p > < div class = "quiz - options" > < div class = "option - group" > < label > < input type = "number" id = "questionsCount" value = "20" min = "5" max = "25" style = "width:60px;padding:0.5rem;" > < strong > Nombre de questions < / strong > < / label > < / div > < / div > < div class = "filters - section" > < div class = "filters - title" > 🎯 Filtrer par type (tous sélectionnés par défaut) < / div > < div class = "filters - grid" > < div class = "filter - group" > < h3 > Type de question < / h3 > < div class = "filter - options" id = "typesContainer" > < / div > < / div > < div class = "filter - group" > < h3 > Thème < / h3 > < div class = "filter - options" id = "themesContainer" > < / div > < / div > < / div > < / div > < div class = "quiz - actions" > < button class = "btn btn - secondary" onclick = "window.backToSelection()" > ← Changer de quiz < / button > < button class = "btn btn - primary" onclick = "window.startMcqQuiz()" > Démarrer le Quiz < / button > < / div > < / div > `;}function populatePharmaCourseFilters(){const families = quizInstance.getFamilies();const familiesContainer = document.getElementById('familiesContainer');familiesContainer.innerHTML = '';families.forEach(family = > {const label = document.createElement('label');label.className = 'filter - label';label.innerHTML = ` < input type = "checkbox" class = "family - checkbox" value = "${family}" checked > ${family}`;familiesContainer.appendChild(label);});const domains = quizInstance.getAvailableDomains();const domainsContainer = document.getElementById('domainsContainer');domainsContainer.innerHTML = '';domains.forEach(domain = > {const label = document.createElement('label');label.className = 'filter - label';label.innerHTML = ` < input type = "checkbox" class = "domain - checkbox" value = "${domain.id}" checked > ${domain.label}`;domainsContainer.appendChild(label);});}function populateMcqFilters(){const types = quizInstance.getTypes();const typesContainer = document.getElementById('typesContainer');typesContainer.innerHTML = '';types.forEach(type = > {const label = document.createElement('label');label.className = 'filter - label';label.innerHTML = ` < input type = "checkbox" class = "type - checkbox" value = "${type}" checked > ${type = = = 'memorization' ? '📚 Mémorisation':'⚠️ Sécurité'}`;typesContainer.appendChild(label);});const themes = quizInstance.getThemes();const themesContainer = document.getElementById('themesContainer');themesContainer.innerHTML = '';themes.forEach(theme = > {const label = document.createElement('label');label.className = 'filter - label';label.innerHTML = ` < input type = "checkbox" class = "theme - checkbox" value = "${theme}" checked > ${theme}`;themesContainer.appendChild(label);});}window.startPharmaCourseQuiz = function(){const selectedFamilies = Array.from(document.querySelectorAll('.family - checkbox:checked')) .map(cb = > cb.value);const selectedDomains = Array.from(document.querySelectorAll('.domain - checkbox:checked')) .map(cb = > cb.value);const count = parseInt(document.getElementById('questionsCount').value);quizInstance.generateFilteredQuiz(count,selectedFamilies,selectedDomains);console.log(`▶ Quiz Pharmacologie démarré:${count}questions`);displayPharmaCourseQuestion();showScreen('quizScreen');};window.startMcqQuiz = function(){const selectedTypes = Array.from(document.querySelectorAll('.type - checkbox:checked')) .map(cb = > cb.value);const selectedThemes = Array.from(document.querySelectorAll('.theme - checkbox:checked')) .map(cb = > cb.value);const count = parseInt(document.getElementById('questionsCount').value);quizInstance.generateQuiz(count,{type:selectedTypes,theme:selectedThemes});console.log(`▶ Quiz Antibiotiques démarré:${count}questions`);displayMcqQuestion();showScreen('quizScreen');};function populateFilters(){const families = quizInstance.getFamilies();const familiesContainer = document.getElementById('familiesContainer');familiesContainer.innerHTML = '';families.forEach(family = > {const label = document.createElement('label');label.className = 'filter - label';label.innerHTML = ` < input type = "checkbox" class = "family - checkbox" value = "${family}" checked > ${family}`;familiesContainer.appendChild(label);});const domains = quizInstance.getAvailableDomains();const domainsContainer = document.getElementById('domainsContainer');domainsContainer.innerHTML = '';domains.forEach(domain = > {const label = document.createElement('label');label.className = 'filter - label';label.innerHTML = ` < input type = "checkbox" class = "domain - checkbox" value = "${domain.id}" checked > ${domain.label}`;domainsContainer.appendChild(label);});}function showScreen(screenId){document.querySelectorAll('.screen').forEach(screen = > {screen.classList.remove('active');});document.getElementById(screenId).classList.add('active');}function displayPharmaCourseQuestion(){const question = quizInstance.getCurrentQuestion();const total = quizInstance.currentQuiz.length;const current = quizInstance.currentQuestionIndex + 1;document.getElementById('questionCounter').textContent = `${current} / ${total}`;const progress = (current / total) * 100;document.getElementById('progressFill').style.width = `${progress}%`;let html = ` < div class = "question" > < span class = "question - type" > Vrai / Faux < / span > < div class = "question - text" > ${question.question} < / div > `;const isAnswered = question.userAnswer ! = = null;html + = ` < div class = "vrai - faux - group" > < button class = "btn - vf ${isAnswered & & question.userAnswer = = = true ? 'selected':''}" onclick = "window.selectVFAnswer(true)" > ✓ Vrai < / button > < button class = "btn - vf ${isAnswered & & question.userAnswer = = = false ? 'selected':''}" onclick = "window.selectVFAnswer(false)" > ✗ Faux < / button > < / div > `;html + = ` < / div > `;document.getElementById('questionContent').innerHTML = html;updateNavigationButtons(current,total,'pharmacy');}function displayMcqQuestion(){const question = quizInstance.getCurrentQuestion();const total = quizInstance.currentQuiz.length;const current = quizInstance.currentQuestionIndex + 1;document.getElementById('questionCounter').textContent = `${current} / ${total}`;const progress = (current / total) * 100;document.getElementById('progressFill').style.width = `${progress}%`;let html = ` < div class = "question" > < span class = "question - type" > ${question.type = = = 'memorization' ? '📚 Mémorisation':'⚠️ Sécurité'} < / span > < div class = "question - text" > ${question.variante} < / div > < div class = "answers" > `;const selectedAnswer = quizInstance.userAnswers[quizInstance.currentQuestionIndex];question.reponsesMelangees.forEach((answer,index) = > {const isSelected = selectedAnswer = = = answer;const ansId = `answer - ${quizInstance.currentQuestionIndex} - ${index}`;html + = ` < div class = "answer - option ${isSelected ? 'selected':''}" style = "background:${isSelected ? '#fff5f8':'white'};border - color:${isSelected ? '#fa709a':'#e0e0e0'};" onclick = "window.selectMcqAnswer('${answer.replace( / ' / g,"\\'")}')" > < input type = "radio" id = "${ansId}" name = "answer" value = "${answer}" ${isSelected ? 'checked':''} > < label for = "${ansId}" style = "cursor:pointer;margin:0;padding:0;flex:1;" > ${answer} < / label > < / div > `;});html + = ` < / div > < / div > `;document.getElementById('questionContent').innerHTML = html;updateNavigationButtons(current,total,'mcq');}function updateNavigationButtons(current,total,quizType){const prevBtn = document.querySelector('button[onclick = "window.previousQuestion()"]');const nextBtn = document.getElementById('nextBtn');prevBtn.disabled = current = = = 1;if (current = = = total){nextBtn.textContent = 'Terminer le quiz →';nextBtn.onclick = function(){window.finishQuiz();};}else{nextBtn.textContent = 'Suivant →';nextBtn.onclick = function(){window.nextQuestion();};}}window.startQuiz = function(){const selectedFamilies = Array.from(document.querySelectorAll('.family - checkbox:checked')) .map(cb = > cb.value);const selectedDomains = Array.from(document.querySelectorAll('.domain - checkbox:checked')) .map(cb = > cb.value);const count = parseInt(document.getElementById('questionsCount').value);quizInstance.generateFilteredQuiz(count,selectedFamilies,selectedDomains);console.log(`▶ Quiz démarré:`);console.log(` - ${count}questions`);console.log(` - Familles:${selectedFamilies.join(',')}`);console.log(` - Domaines:${selectedDomains.join(',')}`);displayPharmaCourseQuestion();showScreen('quizScreen');}window.selectVFAnswer = function(value){const question = quizInstance.getCurrentQuestion();question.userAnswer = value;document.querySelectorAll('.btn - vf').forEach(btn = > {btn.classList.remove('selected');});event.target.classList.add('selected');};window.selectMcqAnswer = function(answer){quizInstance.setAnswer(answer);document.querySelectorAll('.answer - option').forEach(opt = > {opt.classList.remove('selected');opt.style.background = 'white';opt.style.borderColor = '#e0e0e0';});event.target.closest('.answer - option').classList.add('selected');event.target.closest('.answer - option').style.background = '#fff5f8';event.target.closest('.answer - option').style.borderColor = '#fa709a';};window.nextQuestion = function(){if (quizInstance.userAnswers[quizInstance.currentQuestionIndex] = = = null){alert('⚠️ Veuillez sélectionner une réponse');return;}if (quizInstance.nextQuestion()){displayMcqQuestion();}};window.previousQuestion = function(){if (quizInstance.previousQuestion()){displayMcqQuestion();}};window.finishQuiz = function(){if (quizInstance.userAnswers[quizInstance.currentQuestionIndex] = = = null){alert('⚠️ Veuillez sélectionner une réponse');return;}quizInstance.calculateScore();displayResults();};function displayResults(){let results,percentage;results = quizInstance.getResults();percentage = Math.round((quizInstance.score / quizInstance.currentQuiz.length) * 100);document.getElementById('scoreDisplay').textContent = `${quizInstance.score} / ${quizInstance.currentQuiz.length}`;document.getElementById('scorePercentage').textContent = `${percentage}%`;let feedback,className;if (percentage > = 90){feedback = '🌟 Excellent ! Vous avez une excellente compréhension ! ';className = 'excellent';}else if (percentage > = 75){feedback = '👍 Très bon ! Continuez comme ça ! ';className = 'good';}else if (percentage > = 60){feedback = '📚 Correct,continuez à réviser ! ';className = 'average';}else{feedback = '💪 Ne vous découragez pas,pratiquez encore ! ';className = 'poor';}document.getElementById('feedback').textContent = feedback;document.getElementById('feedback').className = `results - feedback ${className}`;let reviewHtml = '';results.forEach((result,index) = > {reviewHtml + = ` < div class = "review - question ${result.isCorrect ? 'correct':'incorrect'}" > < div class = "review - q - number" > Question ${index + 1}${result.isCorrect ? '✓':'✗'} < / div > < div class = "review - q - text" > < strong > ${result.question} < / strong > < / div > < div class = "review - answers - info" > < div class = "review - answer - user" > Votre réponse: < strong > ${result.userAnswer} < / strong > < / div > ${ ! result.isCorrect ? ` < div class = "review - answer - correct" > Bonne réponse: < strong > ${result.correctAnswer} < / strong > < / div > `:''} < / div > < / div > `;});document.getElementById('reviewAnswers').innerHTML = reviewHtml;showScreen('resultsScreen');}window.backToSelection = function(){currentQuizType = null;quizInstance = null;showScreen('quizSelectionScreen');};window.resetQuiz = function(){window.location.href = '. / home.html';};window.retakeQuiz = function(){setupMcqUI();populateMcqFilters();quizInstance.reset();showScreen('startScreen');};function showError(message){const loadingScreen = document.getElementById('loadingScreen');loadingScreen.innerHTML = ` < div class = "loading" > < p style = "color:#dc3545;font - weight:600;" > ${message} < / p > < button class = "btn btn - secondary" onclick = "window.backToSelection()" style = "margin - top:1rem;" > Retour < / button > < / div > `;}window.openQuiz = function(){window.location.href = '. / quiz.html';};
let pharmaQuiz = null;let currentAnswerSelected = null;document.addEventListener('DOMContentLoaded',async () = > {console.log('📚 Initialisation du quiz pharmacologie...');pharmaQuiz = new PharmaQuiz();showScreen('loadingScreen');const loaded = await pharmaQuiz.loadPharmaData();if (loaded & & pharmaQuiz.pharmaData.length > 0){console.log('✓ Données chargées,quiz prêt');populateFilters();showScreen('startScreen');}else{showError('❌ Erreur:Impossible de charger les données pharmacologiques');}});function populateFilters(){const families = pharmaQuiz.getFamilies();const familiesContainer = document.getElementById('familiesContainer');familiesContainer.innerHTML = '';families.forEach(family = > {const label = document.createElement('label');label.className = 'filter - label';label.innerHTML = ` < input type = "checkbox" class = "family - checkbox" value = "${family}" checked > ${family}`;familiesContainer.appendChild(label);});const domains = pharmaQuiz.getAvailableDomains();const domainsContainer = document.getElementById('domainsContainer');domainsContainer.innerHTML = '';domains.forEach(domain = > {const label = document.createElement('label');label.className = 'filter - label';label.innerHTML = ` < input type = "checkbox" class = "domain - checkbox" value = "${domain.id}" checked > ${domain.label}`;domainsContainer.appendChild(label);});}function showScreen(screenId){document.querySelectorAll('.screen').forEach(screen = > {screen.classList.remove('active');});document.getElementById(screenId).classList.add('active');}window.startQuiz = function(){const selectedFamilies = Array.from(document.querySelectorAll('.family - checkbox:checked')) .map(cb = > cb.value);const selectedDomains = Array.from(document.querySelectorAll('.domain - checkbox:checked')) .map(cb = > cb.value);const count = parseInt(document.getElementById('questionsCount').value);pharmaQuiz.generateFilteredQuiz(count,selectedFamilies,selectedDomains);console.log(`▶ Quiz démarré:`);console.log(` - ${count}questions`);console.log(` - Familles:${selectedFamilies.join(',')}`);console.log(` - Domaines:${selectedDomains.join(',')}`);displayQuestion();showScreen('quizScreen');};function displayQuestion(){const question = pharmaQuiz.getCurrentQuestion();const total = pharmaQuiz.currentQuiz.length;const current = pharmaQuiz.currentQuestionIndex + 1;document.getElementById('questionCounter').textContent = `${current} / ${total}`;const progress = (current / total) * 100;document.getElementById('progressFill').style.width = `${progress}%`;let html = ` < div class = "question" > < span class = "question - type" > Vrai / Faux < / span > < div class = "question - text" > ${question.question} < / div > `;const isAnswered = question.userAnswer ! = = null;html + = ` < div class = "vrai - faux - group" > < button class = "btn - vf ${isAnswered & & question.userAnswer = = = true ? 'selected':''}" onclick = "window.selectVFAnswer(true)" > ✓ Vrai < / button > < button class = "btn - vf ${isAnswered & & question.userAnswer = = = false ? 'selected':''}" onclick = "window.selectVFAnswer(false)" > ✗ Faux < / button > < / div > `;html + = ` < / div > `;document.getElementById('questionContent').innerHTML = html;document.querySelector('button[onclick = "window.previousQuestion()"]').disabled = current = = = 1;const nextBtn = document.getElementById('nextBtn');if (current = = = total){nextBtn.textContent = 'Terminer le quiz →';nextBtn.onclick = function(){window.finishQuiz();};}else{nextBtn.textContent = 'Suivant →';nextBtn.onclick = function(){window.nextQuestion();};}const firstAnswer = document.querySelector('.btn - vf');if (firstAnswer) firstAnswer.focus();};window.selectAnswer = function(element){};window.selectVFAnswer = function(value){const question = pharmaQuiz.getCurrentQuestion();question.userAnswer = value;document.querySelectorAll('.btn - vf').forEach(btn = > {btn.classList.remove('selected');});event.target.classList.add('selected');console.log('✓ Vrai / Faux sélectionné:',value);};window.nextQuestion = function(){const question = pharmaQuiz.getCurrentQuestion();if (question.userAnswer = = = null){alert('⚠️ Veuillez sélectionner une réponse avant de continuer');return;}if (pharmaQuiz.nextQuestion()){displayQuestion();}};window.previousQuestion = function(){if (pharmaQuiz.currentQuestionIndex > 0){pharmaQuiz.currentQuestionIndex - - ;displayQuestion();}};window.finishQuiz = function(){const question = pharmaQuiz.getCurrentQuestion();if (question.userAnswer = = = null){alert('⚠️ Veuillez sélectionner une réponse avant de terminer');return;}pharmaQuiz.score = 0;pharmaQuiz.currentQuiz.forEach(q = > {const isCorrect = pharmaQuiz.normalizeAnswer(q.userAnswer) = = = pharmaQuiz.normalizeAnswer(q.correctAnswer);if (isCorrect) pharmaQuiz.score + + ;});displayResults();};function displayResults(){const results = pharmaQuiz.getResults();document.getElementById('scoreDisplay').textContent = `${results.score} / ${results.total}`;document.getElementById('scorePercentage').textContent = `${results.percentage}%`;let feedback,className;if (results.percentage > = 90){feedback = '🌟 Excellent ! Vous maîtrisez très bien la pharmacologie ! ';className = 'excellent';}else if (results.percentage > = 75){feedback = '👍 Très bon ! Vous avez de bonnes connaissances.';className = 'good';}else if (results.percentage > = 60){feedback = '📚 Correct,mais continuez à réviser ! ';className = 'average';}else{feedback = '💪 Continuez vos efforts,la pratique améliorera votre score ! ';className = 'poor';}document.getElementById('feedback').textContent = feedback;document.getElementById('feedback').className = `results - feedback ${className}`;let reviewHtml = '';results.questions.forEach((question,index) = > {const isCorrect = pharmaQuiz.normalizeAnswer(question.userAnswer) = = = pharmaQuiz.normalizeAnswer(question.correctAnswer);reviewHtml + = ` < div class = "review - question ${isCorrect ? 'correct':'incorrect'}" > < div class = "review - q - number" > Question ${index + 1}${isCorrect ? '✓':'✗'} < / div > < div class = "review - q - text" > < strong > ${question.question} < / strong > < / div > < div class = "review - answers - info" > < div class = "review - answer - user" > Votre réponse: < strong > ${question.userAnswer} < / strong > < / div > ${ ! isCorrect ? ` < div class = "review - answer - correct" > Bonne réponse: < strong > ${question.correctAnswer} < / strong > < / div > `:''} < div class = "explanation" > ${question.explanation} < / div > < / div > < / div > `;});document.getElementById('reviewAnswers').innerHTML = reviewHtml;showScreen('resultsScreen');}window.resetQuiz = function(){pharmaQuiz.reset();currentAnswerSelected = null;window.location.href = '. / home.html';};window.retakeQuiz = function(){showScreen('startScreen');};function showError(message){document.getElementById('loadingScreen').innerHTML = ` < div class = "loading" > < p style = "color:#dc3545;font - weight:600;" > ${message} < / p > < p style = "margin - top:1rem;color:#999;" > Assurez - vous que le fichier pharmaco.csv est présent dans src / data / < / p > < button class = "btn btn - secondary" onclick = "window.location.href = '. / home.html'" > Retour < / button > < / div > `;}window.openPharmaQuiz = function(){window.location.href = '. / quiz.html';};
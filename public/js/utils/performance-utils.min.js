class PerformanceMonitor{constructor(){this.metrics = {firestoreReads:0,firestoreWrites:0,cacheHits:0,cacheMisses:0,operations:[]};this.sessionStart = Date.now();}logFirestoreRead(collection,count,duration){this.metrics.firestoreReads + = count;this.metrics.operations.push({type:'read',collection,count,duration,timestamp:Date.now()});console.log(`📖 Firestore read:${collection}(${count}docs,${duration}ms)`);}logFirestoreWrite(collection,count,duration){this.metrics.firestoreWrites + = count;this.metrics.operations.push({type:'write',collection,count,duration,timestamp:Date.now()});console.log(`📝 Firestore write:${collection}(${count}docs,${duration}ms)`);}logCacheHit(key,source = 'memory'){this.metrics.cacheHits + + ;console.log(`✅ Cache hit:${key}(${source})`);}logCacheMiss(key){this.metrics.cacheMisses + + ;console.log(`❌ Cache miss:${key}`);}getReport(){const sessionDuration = Date.now() - this.sessionStart;const totalOperations = this.metrics.firestoreReads + this.metrics.firestoreWrites;const cacheTotal = this.metrics.cacheHits + this.metrics.cacheMisses;const cacheHitRate = cacheTotal > 0 ? (this.metrics.cacheHits / cacheTotal * 100).toFixed(1):0;return{sessionDuration:`${(sessionDuration / 1000).toFixed(1)}s`,firestoreReads:this.metrics.firestoreReads,firestoreWrites:this.metrics.firestoreWrites,totalOperations,cacheHits:this.metrics.cacheHits,cacheMisses:this.metrics.cacheMisses,cacheHitRate:`${cacheHitRate}%`,recentOperations:this.metrics.operations.slice( - 10)};}logReport(){console.log('📊 = = = RAPPORT DE PERFORMANCE = = = ');console.table(this.getReport());}reset(){this.metrics = {firestoreReads:0,firestoreWrites:0,cacheHits:0,cacheMisses:0,operations:[]};this.sessionStart = Date.now();}}class BatchLoader{constructor(db,perfMonitor){this.db = db;this.perfMonitor = perfMonitor;this.batchSize = 30; / / Limite Firestore pour les requêtes 'in'}async loadDocuments(collectionPath,docIds){if ( ! docIds | | docIds.length = = = 0){return{};}const startTime = Date.now();const results = {};try{const{getDocs,query,collection,where,documentId} = await import('https: / / www.gstatic.com / firebasejs / 10.7.0 / firebase - firestore.js');const batches = [];for (let i = 0;i < docIds.length;i + = this.batchSize){batches.push(docIds.slice(i,i + this.batchSize));}const batchPromises = batches.map(async (batch) = > {const q = query( collection(this.db,collectionPath),where(documentId(),'in',batch) );const snapshot = await getDocs(q);const batchResults = {};snapshot.forEach(doc = > {batchResults[doc.id] = doc.data();});return batchResults;});const batchResultsArray = await Promise.all(batchPromises);batchResultsArray.forEach(batchResult = > {Object.assign(results,batchResult);});const duration = Date.now() - startTime;this.perfMonitor?.logFirestoreRead(collectionPath,docIds.length,duration);console.log(`✅ Batch load:${docIds.length}docs en ${batches.length}batch(es) (${duration}ms)`);return results;}catch (error){console.error('Erreur batch load:',error);return{};}}async loadCollectionPaginated(collectionPath,limit = 20,lastDoc = null){const startTime = Date.now();try{const{getDocs,query,collection,orderBy,startAfter,limit:firestoreLimit} = await import('https: / / www.gstatic.com / firebasejs / 10.7.0 / firebase - firestore.js');let q = query( collection(this.db,collectionPath),orderBy('__name__'), / / Ordre par ID de document firestoreLimit(limit + 1) / / + 1 pour savoir s'il y a une page suivante );if (lastDoc){q = query(q,startAfter(lastDoc));}const snapshot = await getDocs(q);const docs = [];let newLastDoc = null;let hasMore = false;snapshot.forEach((doc,index) = > {if (index < limit){docs.push({id:doc.id,...doc.data()});newLastDoc = doc;}else{hasMore = true;}});const duration = Date.now() - startTime;this.perfMonitor?.logFirestoreRead(collectionPath,docs.length,duration);return{docs,lastDoc:newLastDoc,hasMore};}catch (error){console.error('Erreur pagination:',error);return{docs:[],lastDoc:null,hasMore:false};}}}class TermPaginator{constructor(allTerms,pageSize = 20){this.allTerms = allTerms;this.pageSize = pageSize;this.currentPage = 0;this.loadedTerms = [];}loadNext(){const start = this.currentPage * this.pageSize;const end = start + this.pageSize;const nextBatch = this.allTerms.slice(start,end);if (nextBatch.length > 0){this.loadedTerms.push(...nextBatch);this.currentPage + + ;console.log(`📄 Page ${this.currentPage}chargée (${nextBatch.length}termes)`);return nextBatch;}return [];}async preloadNext(){const start = (this.currentPage + 1) * this.pageSize;const end = start + this.pageSize;if (start < this.allTerms.length){return new Promise(resolve = > {setTimeout(() = > {console.log(`🔄 Préchargement page ${this.currentPage + 2}`);resolve(true);},100);});}return false;}hasMore(){return this.currentPage * this.pageSize < this.allTerms.length;}getRemainingCount(){return Math.max(0,this.allTerms.length - this.loadedTerms.length);}reset(){this.currentPage = 0;this.loadedTerms = [];}}window.PerformanceMonitor = PerformanceMonitor;window.BatchLoader = BatchLoader;window.TermPaginator = TermPaginator;